-r requirements.txt

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-mock>=3.14

# Linting / formatting (ruff replaces both black and isort)
ruff>=0.4.4
mypy>=1.10

# Dev workflow
pre-commit>=3.7
